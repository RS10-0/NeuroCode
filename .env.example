# Frontend environment (Vite)
#
# Copy to .env.local and fill in. Only VITE_-prefixed values are
# exposed to the browser, so never put a model provider key here.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
